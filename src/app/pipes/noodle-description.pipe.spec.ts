import { NoodleDescriptionPipe } from './noodle-description.pipe';

describe('NoodleDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new NoodleDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
